apiVersion: v1
kind: Service
metadata:
  name: exceads-dev-tomcat-service
  namespace: biocomplexity
spec:
  selector:
    app: exceads
    environment: dev
    component: tomcat
  ports:
    - protocol: TCP
      port: 8080
      targetPort: http-exceads
